pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                timeout(time: 30, unit: 'MINUTES') {
                    echo 'Building...'
                    // Simulate a long-running task
                }
            }
        }
    }
}

